# Test-only dependencies. The runtime image doesn't need these; see Dockerfile.
-r requirements.txt

pytest==8.3.3
httpx==0.27.2          # required by fastapi.testclient

# Needed by tests/test_ingest.py. Without these the whole suite fails to
# collect, not just the ingest tests — a missing import at collection time
# stops pytest before it runs anything.
numpy==2.1.3
rasterio==1.4.3
PyYAML==6.0.2
h3==4.1.2

# Only the ingest benchmark loads Postgres; the tests do not. Pinned here so
# docs/INGEST-BENCHMARK.md reproduces from a clean checkout.
psycopg[binary]==3.3.4
